# KISS v0.2.0 — build brief

Live now: v0.1.0 at https://app.sendkiss.online
Do NOT merge to production. Open a PR only (or leave commits on a branch). No DNS, no spend, no public posts.

## Must fix in this PR

1. Photo sync phone vs desktop: same phone number must show the same photo. Client today over-uses localStorage (`kiss-me-v2`, `kiss-id-v1`). Source of truth = Neon `phone_book.photo` — write on photo change, read on boot/hydrate for that phone.
2. Logout: LEAVE / signOut must clear those local keys and return to the phone gate.
3. Orbit without QA ghosts: hydrate orbit from real DB users/kisses only (QA was deleted server-side; stale local orbit still shows).
4. Design direction (if time): personal full-screen background (home = your photo; catch = the other person's world); first-kiss card like Naor's mock (Kiss back / Super / Flood); less "AI template" look.

## Out of scope

Google/X OAuth redirect, contacts/WhatsApp picker, store wrapping, domain/DNS, any merge to main/prod without explicit תמזג from Naor.

## Done when

Branch with the above + CHANGELOG note for v0.2.0. Report what changed in Hebrew one short paragraph.
